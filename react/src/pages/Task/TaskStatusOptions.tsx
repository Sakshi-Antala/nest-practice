import { IOptions } from "../../components/Form/forms.model";

const taskStatusOptions: IOptions[] = [
    { label: 'Active', value: 'ACTIVE' },
    { label: 'InActive', value: 'INACTIVE' }
];

export default taskStatusOptions;